# agents/scheduler.py
def schedule_tasks(tasks, calendar_events):
    """
    Placeholder scheduler.
    Future: map tasks into calendar respecting gym, wake time, buffers.
    """
    return {"scheduled": tasks, "calendar": calendar_events}
